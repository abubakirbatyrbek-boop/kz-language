// Supabase browser configuration.
// Replace the two placeholder strings with values from your Supabase project.
// Use the Publishable key (or legacy anon key), NOT the service_role key.
window.SUPABASE_CONFIG = {
  url: 'https://YOUR-PROJECT.supabase.co',
  publishableKey: 'YOUR_SUPABASE_PUBLISHABLE_KEY'
};
